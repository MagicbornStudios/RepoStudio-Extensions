# Assistant Only Studio Example

Reference-only consumer studio example extracted from `apps/consumer-studio`.

This example shows a minimal chat surface with shared companion runtime components from `@forge/dev-kit`.

## Source provenance

- Monorepo path: `apps/consumer-studio`
- Snapshot files copied into `src/`