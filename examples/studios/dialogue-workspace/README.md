# Dialogue Workspace Example

Reference-only Studio example extracted from `apps/studio`.

This is **not installable** as a Repo Studio runtime extension. It is intended for browsing and adaptation while building host-rendered extensions.

## Source provenance

- Monorepo path: `apps/studio/components/workspaces/DialogueWorkspace.tsx`
- Snapshot copied into `src/DialogueWorkspace.tsx`