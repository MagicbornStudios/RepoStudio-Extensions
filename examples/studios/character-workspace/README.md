# Character Workspace Example

Reference-only Studio example extracted from `apps/studio`.

This is **not installable** as a Repo Studio runtime extension. It is provided as source material for extension authors who want to adapt Character-domain workspace flows into project-scoped extensions.

## Source provenance

- Monorepo path: `apps/studio/components/workspaces/CharacterWorkspace.tsx`
- Snapshot copied into `src/CharacterWorkspace.tsx`